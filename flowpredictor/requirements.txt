# Flowpredictor -- pinned runtime environment used to produce the published results.
# Python 3.10.12 (tomli is required because tomllib is only in the stdlib from 3.11).

numpy==1.25.2
pandas==2.1.0
scipy==1.11.2
scikit-learn==1.3.0
tensorflow==2.15.1
xgboost==2.0.0
tomli==1.2.2

# Figures
matplotlib==3.7.2
seaborn==0.13.2

# Optional: NSE/KGE through hydroeval in the per-model metric registry
# (the published fold metrics are computed in src/evaluation/metrics.py).
hydroeval==0.1.0

# Test suite
pytest==6.2.5
